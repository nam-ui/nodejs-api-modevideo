import RatingMongo from "./rating.dto";
import { IRating } from "./rating.interface";

export default class RatingService {
    private constructor() { }

    static create() {
        return "id-descriptions"
    }
}