import StatusMongo from "./status.dto";
import { IStatus } from "./status.interface";

export default class StatusService {
    private constructor() { }

    static create() {
        return "id-descriptions"
    }
}